import 'package:flutter/material.dart';

class InputTile extends StatelessWidget {
  InputTile({@required this.hintText});

  final String hintText;
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
